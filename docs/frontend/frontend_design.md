# Ouroboros 前端设计归档

## 文档边界

本文只定义前端页面、视觉语言、交互需求和页面级数据需求。

- 架构边界以 [../Arch/arch.md](../Arch/arch.md) 的 Layer 4 为准。
- REST、WebSocket、错误码和断线恢复以 [../web_api/README.md](../web_api/README.md)、[../web_api/control_rest.md](../web_api/control_rest.md)、[../web_api/realtime_ws.md](../web_api/realtime_ws.md)、[../web_api/visibility_and_errors.md](../web_api/visibility_and_errors.md) 为准。
- 内部频道来源和可见性转换以 [../internal_contracts/channel_routing.md](../internal_contracts/channel_routing.md) 和 [../internal_contracts/module_interface_registry.md](../internal_contracts/module_interface_registry.md) 为准。
- 本文不得新增 Web API 字段、内部频道或状态归属；发现页面需求缺字段时，先同步更新 Web API 文档。

## 目标

Ouroboros 前端定位为“昂贵、可审计的机构级金融沙盘推演工具”。前端不是营销站，也不是普通 SaaS 仪表盘；它应像彭博终端、工程蓝图和做空机构报告的混合体。

第一版前端按完整沙盘推演生命周期设计 5 个核心页面：

1. 沙盘初始化控制台
2. 实时推演画板
3. 智能体解剖室
4. 时间轴剧本管理
5. 最终判决与漏洞白皮书

前端访问路径固定为 Web API 层。REST 通过 `WebApiGateway`，实时事件通过 `FrontendRealtimeGateway`。前端不得直连 Redis、内部 Pub/Sub、Layer 0/1/2/3 或 Agent runtime。

普通前端不得展示原始 `thought`、Prompt、私有记忆或 Agent 原始 payload。若未来需要沙盒外诊断能力，必须独立设计权限、审计日志和接口，不复用普通页面。

## 总体风格

采用“瑞士国际主义排版 + 先锋金融终端风”：高对比工程化财经终端 + 报纸编辑部版式。

视觉目标：

- 严肃、冷静、昂贵、可审计。
- 像白底黑字的机构交易终端、工程图纸和财经报告，少量克莱因蓝形成高强度撞色。
- 信息密度高，但所有边界都由严格网格切割。
- 不用互联网产品常见的柔和卡片、渐变、玻璃拟态、圆角和投影。

基础色：

| 颜色 | 值 | 用途 |
| :--- | :--- | :--- |
| 纯白 | `#ffffff` | 页面主体、图表背景、报告纸面、高密度数据区 |
| 纯黑 | `#000000` | 主要文字、边框、表格线、下跌实体 |
| 克莱因蓝 | `#002fa7` | 小面积高强度强调：运行态、选中线、关键按钮、关键曲线、预警标记 |
| 警戒红 | `#dc2626` | 风险、失败、异常，仅小面积使用 |
| 增长绿 | `#10b981` | 正向状态，仅小面积使用 |

面积原则：

- 85% 以上屏幕面积应为纯白或白底黑线框。
- 克莱因蓝只小面积使用；不得铺满导航、页眉、整列侧栏或大块内容背景。
- 黑色用于字体、边框、表格线和金融语义，不做大面积背景。
- 白色用于承载高密度数据、图表和长文本阅读区。
- 红绿只服务金融语义，不做装饰。

字体分工：

| 用途 | 字体 | 使用范围 |
| :--- | :--- | :--- |
| 叙事标题 | Georgia, Times New Roman, SimSun, serif | Masthead、报告标题、页面主标题 |
| 数据与机器状态 | Courier New, JetBrains Mono, monospace | Tick、时间、价格、Agent id、错误码、事件流 |
| UI 控件与正文 | Helvetica Neue, Arial, sans-serif | 导航、按钮、标签、长文本、普通说明 |

页面军规：

1. 用边框代替间距。模块之间优先使用 `border-right`、`border-bottom` 切开。
2. 全系统默认 `border-radius: 0`。
3. 禁止阴影、发光、模糊、渐变、装饰性圆点。
4. 白底上使用纯黑 1px/2px 线；蓝底只用于小块区域，文字优先黑色。
5. Hover 只做瞬间颜色反转，不做弹跳、呼吸、漂浮动效。
6. 图表不使用默认主题，不使用面积渐变和灰色网格。

## UI 系统落地规则

第一版前端按桌面专业工具设计，不按移动端优先设计。目标分辨率为 `1440px` 及以上；`1280px` 到 `1439px` 保持可用但允许信息更紧；低于 `1280px` 时进入压缩模式，保留横向滚动，不重排成移动端卡片流。

### 字号层级

| 层级 | 字体 | 字号 | 行高 | 用途 |
| :--- | :--- | :--- | :--- | :--- |
| Masthead | Georgia, Times New Roman, SimSun, serif | `32px` | `40px` | 页面主标题、白皮书标题 |
| Page title | Georgia, Times New Roman, SimSun, serif | `24px` | `32px` | 页面标题、报告分栏标题 |
| Module title | Helvetica Neue, Arial, sans-serif | `13px` | `18px` | 分区标题、工具栏标签 |
| Body | Helvetica Neue, Arial, sans-serif | `14px` | `22px` | 正文、审计摘要 |
| Dense body | Helvetica Neue, Arial, sans-serif | `12px` | `18px` | 表格、列表、控件说明 |
| Machine | Courier New, JetBrains Mono, monospace | `12px` | `18px` | Tick、价格、Agent id、错误码 |
| Micro data | Courier New, JetBrains Mono, monospace | `10px` | `14px` | 盘口窄条、图表角标 |

约束：

- 不使用随视口变化的字体大小。
- 字间距保持 `0`，不使用负字距。
- 英文、数字和 id 优先使用等宽字体；中文长文本优先使用 Helvetica 系列。
- 模块内标题不得使用 Masthead 尺寸，避免页面内部层级失控。

### 网格与线框

- 页面主体使用 12 列网格作为实现参考，但视觉切割优先服从页面定义的 `20% / 60% / 20%`、`25% / 75%`、三列报告等结构。
- 全局基础间距为 `8px`；密集表格可使用 `4px` 内边距；页面级模块间不靠空白分隔，靠边框分隔。
- 主分区边框使用 `2px` 纯黑；模块内部表格线和图表辅助线使用 `1px` 纯黑。
- 不使用卡片嵌套卡片。重复列表项可使用单层硬边框或 `border-bottom` 分隔。
- 工具栏高度固定为 `40px` 或 `48px`；表格行高默认 `28px`；盘口窄条行高固定 `24px`。

### 基础组件

按钮：

- 主按钮使用克莱因蓝底、黑字、`2px` 黑边框；只允许每页一个主蓝色块。
- 次级按钮使用白底黑字、`1px` 黑边框。
- 危险按钮使用白底黑字、`2px` 警戒红边框，不使用整块红底。
- 禁用态使用白底、黑色 40% 透明度文本、`1px` 黑线，不使用灰色大面积背景。

输入框：

- 高度 `32px` 或 `40px`，直角黑框。
- Focus 态使用 `2px` 克莱因蓝边框。
- 错误态使用 `2px` 警戒红边框，并在字段下方用 `12px` Helvetica 展示短错误原因。

步进器：

- 使用输入框 + 左右硬边小方块。
- `-` 和 `+` 使用等宽字体。
- 每次点击只改变一个离散步长；长按加速不是第一版必须能力。

状态标记：

- 不使用圆角徽章。
- 状态用短横线、左边线、单字符符号或硬边小标签表达。
- `running` 使用克莱因蓝短线；`paused` 使用黑色短线；`failed` 使用警戒红短线；`completed` 使用黑色双线。

Tooltip：

- 白底、黑字、`1px` 黑边框。
- 不使用阴影。
- 内容只解释当前数据或操作，不写产品说明文案。

### 全局交互状态

页面必须实现以下状态视觉，不得由具体页面临时发挥：

| 状态 | 视觉规则 | 适用位置 |
| :--- | :--- | :--- |
| `empty` | 白底黑框，中央使用等宽短句，例如 `NO SESSION` | 无会话、无事件、无图谱 |
| `loading` | 保留最终布局骨架，用 `1px` 黑线占位，不使用骨架灰块动画 | 首次加载、快照恢复 |
| `live` | 当前模块边缘出现克莱因蓝短线，避免大面积蓝色背景 | 运行页、事件流 |
| `paused` | 顶部状态栏保持白底，状态文本变为等宽黑字 | 控制面 |
| `recovering` | 顶部状态栏出现克莱因蓝斜线纹理或短线序列，文案使用 `RECOVERING SNAPSHOT` | WebSocket 断线恢复 |
| `error` | 错误进入事件流，同时关键操作区显示红色硬边错误条 | REST/WS 错误 |
| `terminal` | 页面停止动画与实时刷新，顶部显示完成原因 | 完成、失败、停止 |

错误展示规则：

- `system.error` 必须进入实时推演页事件流。
- 表单校验错误显示在字段附近。
- 控制命令失败显示在操作按钮上方的硬边错误条。
- 不可恢复错误占据当前模块主体，但不破坏全局导航。
- 错误文案只说明事实和下一步操作，不展示内部 payload、Prompt、`thought` 或私有记忆。

## 全局导航

导航像终端模块切换，而不是普通网站导航。

导航项：

- 初始化
- 实时推演
- 智能体
- 时间轴
- 白皮书

导航规则：

- 顶部高度 72px。
- 白底，黑色字体，底部 2px 纯黑线。
- 左侧品牌使用 Georgia：`Ouroboros`
- 右侧模块名使用等宽字体或 Helvetica Bold。
- 当前页面底部用 3px 克莱因蓝线标记。

## 页面 1：沙盘初始化控制台

### 定位

推演开始前的参数配平中心。视觉上像飞行前检查清单、工程蓝图参数表和机构交易前风控单。

### 布局

```text
┌──────────────────────────────────────────────────────────────┐
│ [OUROBOROS // 新建推演]             标的 000001.SH  3021.44    │
├──────────────────────────┬───────────────────────────────────┤
│ 标的与环境 40%            │ 智能体配平 60%                     │
│                          │ ┌────────┬────────┬────────┐      │
│ 标的代码                 │ │ 公募   │ 游资   │ 散户   │      │
│ 时间锚点                 │ │ 雷达图 │ 雷达图 │ 雷达图 │      │
│ 场景编号                 │ │ 参数   │ 参数   │ 参数   │      │
├──────────────────────────┴───────────────────────────────────┤
│ [启动推演序列]                                                │
└──────────────────────────────────────────────────────────────┘
```

顶部：

- 高度约 15vh。
- 左侧 Georgia 大标题：`[OUROBOROS // 新建推演]`
- 右侧可使用小面积克莱因蓝数字块，黑色等宽字体展示真实市场指数或系统时间。

左侧列：标的与环境

- 宽度 40%。
- 白底，1px 黑线框出表单。
- 字段：
  - 标的代码
  - 场景编号
  - 时间锚点
  - 起始节拍
  - 结束节拍
  - 节拍间隔
- 输入框为直角黑框。
- Focus 时边框变 2px 克莱因蓝。

右侧列：智能体配平

- 宽度 60%。
- 三列等宽：
  - 公募机构
  - 游资
  - 散户
- 每个阵营包含：
  - 智能体数量步进器。
  - 资金权重步进器。
  - 信息敏感度步进器。
  - 风险厌恶度步进器。
  - 极简纯线条雷达图。
- 第一版这些控件默认作为 `agent_profile_set` 的可视化预览和本地草稿编辑，不承诺写入后端。若用户调整数值，页面必须显示“本地草案”状态；提交时仍以 Web API 当前支持的 `agent_profile_set` 创建会话。

控件规则：

- 不使用传统 Slider。
- 数值调节使用输入框 + 步进器。
- 步进器按钮是硬边小方块，`-` 和 `+` 使用等宽字体。

雷达图规则：

- 图表框为正方形。
- 雷达网为纯黑 1px 多边形。
- 参数连线为 1px 克莱因蓝曲线或折线。
- 不使用填充面积，不使用渐变。

底部操作：

- 巨大按钮：`[启动推演序列]`
- 默认克莱因蓝底 + 黑字；按钮是页面内主要蓝色块，不再额外铺蓝色背景。
- Hover 变白底 + 黑字，并保留黑色边框。
- 点击后创建会话，但不直接绕过控制面；必须调用 Web API 会话创建接口。

### 第一版实现方案

初始化控制台第一版按“真实会话创建 + 配平预览”实现：

- 左侧 `标的代码`、`场景编号`、`起始节拍`、`结束节拍`、`节拍间隔` 对应 `POST /api/v1/sessions` 请求字段。
- `时间锚点` 作为页面展示字段，第一版不单独提交；若后端后续需要，必须先扩展 Web API。
- 右侧阵营配平从预设 `agent_profile_set` 派生显示；默认配置为 `default_24`。
- 阵营步进器可改变本地预览，但第一版不改变实际创建请求，除非 Web API 明确定义阵营参数字段。
- 当本地预览与实际可提交参数不一致时，启动按钮上方显示硬边提示：`LOCAL PROFILE PREVIEW ONLY`。
- 创建成功后停留在控制台并显示会话号、状态和 WebSocket 地址；用户再进入实时推演页或点击启动。
- 创建失败时不清空表单，在底部操作区显示错误条。

必要控件状态：

| 状态 | 启动按钮 | 表单 | 说明 |
| :--- | :--- | :--- | :--- |
| 未填写必填项 | 禁用 | 可编辑 | 字段下方显示短错误 |
| 创建中 | 禁用 | 锁定 | 按钮文字为 `[创建会话中]` |
| 已创建未启动 | 可进入实时页 | 锁定主要字段 | 显示 `session_id` |
| 创建失败 | 可重试 | 可编辑 | 保留用户输入 |

### 数据来源

- REST `POST /api/v1/sessions`
- REST `GET /api/v1/sessions/{session_id}`
- 可选市场预览来自 WebSocket `market.price` 或 REST `GET /api/v1/sessions/{session_id}/snapshot`。

### 禁止

- 不直接初始化 Layer 0、Layer 3 或 Agent runtime。
- 不允许前端写入 `Order_Input`。
- 不允许前端注入 `Forum_Rumors` 或 `thought`。

## 页面 2：实时推演画板

### 定位

推演过程中的核心监控室。冷酷网格牢笼内，Agent、叙事和价格像粒子轨迹一样碰撞。

### 布局

```text
┌──────────────────────────────────────────────────────────────┐
│ Ouroboros 实时推演     会话 演示会话一     节拍 14     运行中  │
├──────────────┬───────────────────────────────────┬───────────┤
│ 时间轴事件流 │ 共识感染画布                         │ 盘口      │
│ 20% 蓝底      │ 上 60%: 拓扑图谱                   │ 20%       │
│              │ 下 40%: 价格 / 共识曲线             │           │
└──────────────┴───────────────────────────────────┴───────────┘
```

左侧栏：时间轴事件流

- 宽度 20%。
- 白底，黑色等宽字体；只用克莱因蓝左边线或事件编号标记运行态。
- 像报纸窄列，严格按 Tick 向下滚动。
- 展示：
  - `runtime.tick_state`
  - `market.tape_alert`
  - `forum.post`
  - 官方事实注入摘要
  - `system.error` 摘要
- 不展示未来事实。
- 不展示原始 `thought`。

中栏：画布

- 宽度 60%。
- 白底。
- 上 60%：共识感染拓扑图。
- 下 40%：价格走势和共识平均值。

拓扑图规则：

- 允许圆和曲线，但必须是几何工程语言。
- 节点是高密度微型仪表盘，形状只能是绝对正圆形或正方形。
- 节点外侧使用 `1px` 或 `2px` 纯黑/纯白硬描边。
- 节点内部不得写 Agent 名称。
- 节点上半部分作为信念值填充区：
  - 克莱因蓝代表极度看多。
  - 纯黑代表极度看空。
  - 纯白或空心代表观望。
  - 不使用渐变色。
- 节点最底边绘制极细微型进度条，表达仓位暴露 `position exposure`；例如左侧 70% 黑线代表 `P:70%`。
- 标签使用外部引线：从节点拉出一条水平 `1px` 直线，再用等宽字体标注，例如 `[AGT-14] C:0.85 P:70%`。
- 禁止阴影、渐变、发光、呼吸动画。
- 连线使用 1px 纯黑或克莱因蓝贝塞尔曲线。
- 箭头使用锐利几何三角形，不使用圆角箭头。
- 曲线视觉隐喻是粒子碰撞轨迹，不是社交网络图。
- 图谱背景可使用极淡工程网格：纯黑 `1px` 网格，透明度不超过 5%；可在少量交叉点放置极小 `+` 标记。
- 图谱左上角和右下角可用 `10px` 等宽字体显示坐标系水印，例如 `X:-240.50 Y:112.00 // SCALE 1:1`。水印只服务空间定位气质，不承载业务状态。

拓扑交互：

- 点击节点：选中该 Agent，并同步智能体解剖室的当前 Agent 上下文。
- Hover 节点：进入 Spotlight Isolation，只保留该节点、直接影响它的信息源、被它影响的目标节点和相关连线为正常可见；其他节点和连线瞬间降至 10% 透明度。
- Hover 节点 Tooltip：显示 `agent_id`、`agent_type`、`risk_state`、`belief_score`、`position exposure` 和脱敏审计摘要；不得显示原始 `<thought>`、COT、Prompt 或私有记忆。
- 点击节点：右侧数据检视器 `Inspector` 填充该 Agent 的资产、信念、生命周期和可展示因果链。
- 锁定节点：被选中节点外围显示静态方形虚线框 `Targeting Box`；不使用旋转、呼吸或弹跳动画。
- 点击边：高亮该边对应的 `reason_ref` 或 `public_reason`，并在事件流中定位关联事件。
- Hover 边：显示脱敏原因、权重和置信语义；不得显示原始 `thought`。
- 点击因果链：高亮链路相关节点、边和价格区间。
- 默认隐藏低权重边；用户可在画布工具栏切换显示弱相关链路。
- 画布必须提供“重置视图”“只看选中链路”“只看当前 Tick 影响”三个操作。

时间刮擦器：

- 图谱下方放置 Tick 刻度滑块，用于在当前会话的历史 Tick 中前后刮擦。
- 刻度必须清晰硬朗，使用等宽字体标注 `T0`、`T1`、`T2` 等。
- 拖动滑块时，节点信念值、仓位进度条和连线显隐随 Tick 同步更新。
- 刮擦只读取前端已缓存快照/事件或 REST 事件回放；不得请求内部 Redis 或内部 channel。
- 刮擦状态下顶部标记 `REPLAY TICK`，避免与实时运行状态混淆。

控制台同步：

- 当图谱新增强影响连线时，左侧事件流同步高亮对应事件。
- 高亮日志使用等宽短句，例如 `[SYS_ALERT] MASS PANIC DETECTED IN ORBIT 3`。
- 日志文案必须来自可展示事件或脱敏摘要，不得生成私有推理内容。

曲线语义：

| 线型 | 含义 |
| :--- | :--- |
| 实线 | 已确认公开影响 |
| 虚线 | 审计推断链路 |
| 点划线 | 弱相关或低置信链路 |

行情曲线：

- 不使用面积图。
- 价格走势使用 2px 克莱因蓝实线。
- 共识平均值使用 1px 纯黑实线。
- 没有完整坐标轴，只保留极值点的悬浮等宽标签。

右侧栏：微观订单簿

- 宽度 20%。
- 白底。
- 横向切成大量 24px 高窄条。
- 卖盘用警戒红小面积文字。
- 买盘用纯黑或极小面积绿色。
- 数字全部等宽字体。
- 不展示底层真实订单 id 或 Agent 身份绑定意图；只展示 Web API 允许的聚合行情视图。

### 第一版实现方案

实时推演画板第一版按“快照先行 + 事件增量”实现：

- 进入页面时先展示固定三栏骨架。
- 调用快照后填充盘口、Agent 摘要、图谱和因果链。
- WebSocket 连接成功后只追加增量事件，不重置用户当前选中节点。
- 断线时保持最后一帧画面，顶部状态栏进入 `RECOVERING SNAPSHOT`；恢复后按快照重建，再接 `from_seq` 之后事件。
- `system.error` 同时进入事件流和顶部状态条；不可恢复错误冻结画布。

图谱布局第一版由前端确定性计算，不要求后端返回坐标：

- 按 `agent_type` 分区：公募、游资、散户、国家队或其他类型分布在固定扇区。
- 同类型节点按 `agent_id` 稳定排序，确保刷新后位置不跳变。
- `belief_score` 可影响节点到中心距离；`risk_state` 只影响边框或短线标记，不改变坐标。
- 边权重只影响显示优先级和线型，不使用粗线表达。
- 若节点数量超过 48，默认进入降噪模式，只显示高权重边和当前选中链路。
- 信念漂移阈值第一版由前端基于已接收的 `belief_score` 历史值计算，仅用于展示剪枝；不得把该计算结果写回 Web API 或 Agent。

运行控制区第一版必须包含：

- `start`
- `pause`
- `step`
- `stop`
- 当前 `status`
- 当前 `tick_state`
- 当前 `last_seq`

按钮状态按 Web API 状态冲突规则禁用，不允许前端在 `running` 时继续触发 `step`。

### 数据来源

- WebSocket `runtime.tick_state`
- WebSocket `runtime.agent_lifecycle`
- WebSocket `market.price`
- WebSocket `market.tape_alert`
- WebSocket `forum.post`
- WebSocket `audit.graph`
- WebSocket `audit.causal_chain`
- WebSocket `system.error`
- REST `GET /api/v1/sessions/{session_id}/snapshot` 用于断线恢复。

### 禁止

- 不显示 `Order_Input`、`UI_Audit`、`trade_batch`、`risk_result` 原始 payload。
- 不允许 WebSocket 提交订单、发帖、改账或改 Agent 状态。

## 页面 3：智能体解剖室

### 定位

审计单个智能体的状态、资产、信念轨迹和脱敏推理摘要。体现系统可解释性。

### 布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 智能体解剖室     [散户乙]                                    │
├──────────────────┬───────────────────────────────────────────┤
│ 实体名录 25%     │ 四象限工作区 75%                          │
│                  │ ┌──────────────┬────────────────────────┐ │
│ [公募甲]         │ │ 资产表        │ 脱敏审计流              │ │
│ [散户乙]         │ ├──────────────┤                        │ │
│ ...              │ │ 信念轨迹      │                        │ │
│                  │ └──────────────┴────────────────────────┘ │
└──────────────────┴───────────────────────────────────────────┘
```

左侧：实体名录

- 宽度 25%。
- 像列车时刻表。
- 24 个智能体竖排：
  - `[公募甲]`
  - `[散户乙]`
  - `[国家队甲]`
- 默认白底黑字。
- 选中行白底黑字，左侧或底部用克莱因蓝短线标记。
- 高风险智能体行尾用小面积红色标记。

主工作区：四象限

左上：资产表

- 纯表格排版。
- 字段：
  - `cash`
  - `available_cash`
  - `positions`
  - `available_shares`
  - `frozen_shares`
  - `market_value`
  - `equity`
  - `risk_state`
- 数据来自 `agent.account_snapshot` 或 REST 快照。

左下：信念轨迹

- 白底。
- 1px 克莱因蓝曲线。
- 不使用面积填充。
- 标注关键 Tick，例如强平、发帖、盘口异动。

右侧贯穿：脱敏审计室

- 白底。
- 左侧一条 1px 克莱因蓝竖线贯穿到底。
- 展示脱敏审计摘要、公开原因、事件引用、风险标签。
- 字体使用 Helvetica，保证长文本可读。
- 关键逻辑词汇可用克莱因蓝高亮，例如“强平线”“恐慌”“流动性真空”。
- 审计流不得设计成聊天气泡；必须按调查笔录形式展示 `tick_id`、事件引用、公开原因、风险标签。

### COT 边界

普通智能体解剖室不展示原始 `<thought>` 或 COT。它只能展示：

- 脱敏 `public_reason`
- `audit.causal_chain` 摘要
- 可展示事件引用
- 风险状态说明

若后续要展示原始 COT，必须新增沙盒外诊断能力，并满足：

- 独立权限。
- 独立审计日志。
- 不进入普通 Web API 事件回放。
- 不回流 Agent。

### 第一版实现方案

智能体解剖室第一版按“普通审计视图”实现，不做角色权限分层：

- 左侧实体名录从 `snapshot.agents` 初始化，再由 `runtime.agent_lifecycle` 和 `agent.account_snapshot` 增量更新。
- 默认选中风险最高的 Agent；若没有风险状态，选中列表第一项。
- 资产表只展示 Web API 允许的账户审计字段。
- 信念轨迹优先从 `audit.graph.nodes[].belief_score` 的历史缓存绘制；缺历史时只显示当前点和空状态。
- 脱敏审计室从 `audit.causal_chain.steps` 和 `audit.graph.edges[].public_reason` 生成只读审计条目。
- 点击资产表中的 Agent id 或图谱节点时，同步选中同一 Agent。
- 如果某 Agent 没有审计链路，右侧显示 `NO PUBLIC CAUSAL CHAIN`，不得生成推测性文案。

状态视觉：

| 状态 | 展示方式 |
| :--- | :--- |
| `normal` | 黑色短线 |
| `warning` | 克莱因蓝短线 |
| `margin_call` | 警戒红短线 + `MARGIN CALL` |
| `liquidating` | 警戒红双线 + `LIQUIDATING` |
| `terminated` | 黑色双线 + `TERMINATED` |
| `suspended` | 黑色斜线纹理 + `SUSPENDED` |

### 数据来源

- WebSocket `agent.account_snapshot`
- WebSocket `runtime.agent_lifecycle`
- WebSocket `audit.graph`
- WebSocket `audit.causal_chain`
- REST `GET /api/v1/sessions/{session_id}/snapshot` 中的 `agents`

## 页面 4：时间轴剧本管理

### 定位

展示和管理历史切片、官方事实轨、谣言轨和演示剧本。它证明系统不是黑箱，而是可复现实验沙盘。

### 权限边界

时间轴页面属于第一版五页范围，但第一版以只读展示和剧本草案预览为主。它涉及未来 Tick、事实释放和剧本插桩，写入或正式编辑能力必须后续另行设计权限和审计。

### 布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 时间轴剧本管理                                               │
├──────────────────────────────────────────────────────────────┤
│ 时间线: ───○────○────●────△────○────○────────────────────── │
├──────────────────────────────┬───────────────────────────────┤
│ 官方事实轨                    │ 公开传闻轨                    │
│ 报纸式官方事件                 │ 电报式情绪消息                 │
└──────────────────────────────┴───────────────────────────────┘
```

顶部时间轴：

- 高度 20%。
- 一条 2px 黑线横穿屏幕。
- Tick 使用几何圆点。
- 当前 Tick 使用克莱因蓝实心圆。
- 未来插桩预警使用克莱因蓝三角形。

下半区：双轨瀑布流

左半区：官方事实轨

- 报纸头版排版。
- Georgia 大标题。
- 黑白强对比。
- 展示官方新闻、财报、监管函、宏观数据。

右半区：公开传闻轨

- 电报式消息堆叠。
- 等宽时间戳。
- 显示公开论坛消息、情绪指数、传播范围。

第一版只读规则：

- 页面标题区必须显示 `READ ONLY TIMELINE`，避免误导用户认为可编辑运行态。
- 时间轴上的未来 Tick 只显示占位和公开可展示摘要；不得展示未释放事实详情。
- 当前 Tick 之前的事实和传闻可展开详情。
- 当前 Tick 之后的插桩只显示类型、释放 Tick 和可见性，不显示正文。
- 不提供拖拽、删除、保存、发布按钮。
- 可以提供本地草案预览区，但必须标记 `LOCAL DRAFT`，并且刷新页面后允许丢失。

插桩规则：

- 若启用编辑能力，只能生成剧本草案，不能直接绕过 Meta-Orchestrator 或 Chronos 写入运行态。
- 每个插桩必须有：
  - `tick_id`
  - `event_type`
  - `visibility`
  - `source`
  - `audit_reason`
- 插桩不得泄露未来事实给当前 Tick Agent。

### 第一版实现方案

时间轴剧本管理第一版按“只读复盘 + 本地草案预览”实现：

- 顶部时间轴根据会话 `start_tick_id`、`end_tick_id` 和当前 `tick_id` 绘制固定刻度。
- 官方事实轨只展示已经公开释放的事实摘要；没有 Web API 事件时显示空状态。
- 公开传闻轨展示 `forum.post` 和 `market.tape_alert` 的公开文本。
- 草案预览不写入 REST，不写入 WebSocket，不写入 Chronos SSOT。
- 如果后续需要保存草案，必须先新增独立 Web API，并定义权限、审计日志和恢复语义。
- 页面不提供“正式发布插桩”入口。

### 数据来源

- REST `GET /api/v1/sessions/{session_id}` 会话配置。
- Chronos 公开释放记录的 Web API 摘要。
- WebSocket `forum.post`
- WebSocket `market.tape_alert`

### 禁止

- 不允许普通用户直接改 Layer 0 SSOT。
- 不允许把未来 Tick 事实暴露给 Agent。
- 不允许把私有 `thought` 写成官方或谣言轨事件。

## 页面 5：最终判决与漏洞白皮书

### 定位

推演结束后的静态报告页。视觉上像可直接用黑白打印机打印的学术论文、做空机构报告或监管压力测试附件。

### 布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 崩塌概率：87.4%                         2026-05-17 / 演示    │
├──────────────────┬──────────────────────┬───────────────────┤
│ 概述与定性        │ 图表证据              │ 虚拟龙虎榜 / 附录  │
│                  │ 桑基图 / 曲线         │ 表格              │
└──────────────────┴──────────────────────┴───────────────────┘
```

全局规则：

- 全屏白底。
- 不使用动画。
- 可以打印。
- 用经典报纸三列版式。
- 主要内容使用白底黑字和黑色表格线，克莱因蓝只标记关键证据路径。

页眉：

- 左上巨大 Georgia 标题。
- 示例：`崩塌概率：87.4%`
- 右上等宽字体：
  - `session_id`
  - `symbol`
  - `start_tick_id`
  - `end_tick_id`
  - `generated_at`

左栏：概述与定性

- 密集长文。
- 总结压力测试结论。
- 示例：`在第 7 个节拍，散户阵营发生不可逆踩踏。`
- 可使用 Drop Cap，但保持黑白。

中栏：图表证据

- 阵营瓦解路径图。
- 可使用桑基图，但必须保持克制：
  - 背景纯白。
  - 曲线为克莱因蓝或纯黑。
  - 不使用半透明渐变。
  - 节点为硬边矩形或几何圆。

右栏：表格与附录

- 虚拟龙虎榜。
- 智能体风险排行。
- 关键 Tick 列表。
- 只用 1px 黑色表格线。
- 无表头底色，最多用粗线区分表头。

证据阅读路径：

1. 页眉展示结论数字和会话元信息。
2. 左栏给出结论摘要和关键风险判断。
3. 中栏展示崩塌路径、价格曲线和因果链证据。
4. 右栏列出虚拟龙虎榜、风险排行和关键 Tick。
5. 附录保留事件引用，供用户反查事件流。

报告交互：

- 默认静态阅读，不自动刷新。
- 点击关键 Tick 时，高亮对应图表区间和右栏表格行。
- 点击因果链标题时，高亮中栏证据路径。
- 打印模式隐藏全局导航和运行控制，只保留报告内容。
- 不提供“导出结论回写推演”的入口。

### 第一版实现方案

白皮书第一版按“前端汇总展示后端可见事件”实现：

- 崩塌概率第一版只展示后端或快照中已有字段；若 Web API 未提供该字段，显示 `N/A`，不得由前端临时计算。
- 结论摘要可基于 `audit.causal_chain.summary`、`market.end_of_day` 和关键风险状态进行模板化整理，但必须标记为前端展示摘要。
- 虚拟龙虎榜只来自 `market.end_of_day.dragon_tiger`。
- 风险排行来自 `agent.account_snapshot` 或 `snapshot.agents` 的允许字段。
- 关键 Tick 列表来自 `GET /api/v1/sessions/{session_id}/events` 中的可展示事件。
- 桑基图第一版可降级为硬边节点路径图；不得因为图表库默认样式破坏黑白线框风格。

### 数据来源

- REST `GET /api/v1/sessions/{session_id}/snapshot`
- REST `GET /api/v1/sessions/{session_id}/events`
- Web API 可展示事件归档
- WebSocket `audit.causal_chain`
- WebSocket `market.end_of_day`
- WebSocket `agent.account_snapshot` 审计视图

### 禁止

- 不展示原始 COT。
- 不展示 Agent 原始 payload。
- 不展示内部 `UI_Audit`、`Order_Input`、`trade_batch`、`risk_result`。
- 不把报告结论回流 Agent。

## 图谱与曲线规范

### 微型仪表盘节点

允许圆形或正方形节点，但必须满足：

- 形状只能是绝对正圆或绝对正方形。
- 外侧使用 `1px` 或 `2px` 纯黑/纯白硬描边。
- 节点内部是数据仪表，不承载 Agent 名称。
- 上半部分用于信念值填充：克莱因蓝代表强看多，纯黑代表强看空，纯白/空心代表观望。
- 底边使用 `1px` 微型进度条表达仓位暴露。
- 选中态使用静态方形虚线 `Targeting Box`，不得使用旋转动画。
- 不使用阴影、渐变、发光、呼吸动画。
- 标签不塞满圆内，使用引线标注。
- 标签格式示例：`[AGT-14] C:0.85 P:70%`
- 节点半径第一版固定为 `12px` 到 `18px`；不得用大圆表达重要性，避免遮挡。
- 重要性、风险和选中关系通过描边、短线和高亮链路表达。

### 手术刀贝塞尔曲线

允许曲线，但必须满足：

- 线宽 1px。
- 颜色只用纯黑或克莱因蓝。
- 箭头是尖锐几何三角形。
- 不使用粗线、光束、圆角箭头。
- 曲线交叉可以存在，但必须像粒子轨迹或数学图。
- 第一版边宽固定 `1px`；边权重通过显示优先级、线型和 tooltip 表达。
- 默认只显示高置信或当前选中链路相关边，避免图谱变成不可读线团。
- 曲线使用 Cubic Bezier，但控制点必须让线条接近绷紧弓弦，不使用松散波浪形。
- 箭头使用纯色几何等腰三角形，底边平齐，不使用圆角箭头。

### 图谱剪枝规则

第一版必须执行极端剪枝，不渲染全量两两关系：

- 只显示当前 Tick 发生的有效影响。
- 上一 Tick 的影响默认降为 10% 透明度或点状极细虚线。
- 历史边只在用户进入回放、选中因果链或打开历史影响开关时显示。
- 只有读取信息后信念值发生绝对值大于 `0.3` 的漂移，才生成默认可见连线。
- 不画出所有信息读取动作；低影响读取只能进入审计摘要或 tooltip。
- 24 个 Agent 的图谱不得默认展示超过 48 条边。
- 超过阈值时按影响强度、当前 Tick、选中链路优先级裁剪。

### 行情和信念曲线

- 不使用面积图。
- 不使用默认灰色网格。
- 坐标轴实线默认隐藏。
- 数值标签悬浮，使用等宽字体。
- 多线对比使用实线、虚线、点划线，而不是多彩色系。
- 缺少历史数据时显示当前点和 `NO HISTORY` 空状态，不使用假数据补线。
- 极值标签只在 hover 或选中区间出现，避免常驻遮挡曲线。

### K 线图

推荐传统报纸印法：

- 上涨：白色实体 + 黑色描边。
- 下跌：黑色实心。
- 涨跌停或异常：小面积红色标记。
- 背景纯白。
- 不使用圆角蜡烛、阴影、渐变。

### 图表库约束

- 可以使用成熟图表库，但必须关闭默认主题、动画、阴影、渐变、圆角和灰色网格。
- 图表颜色只能来自本文基础色。
- 图表 tooltip 必须使用全局 Tooltip 规则。
- 图表不得在内部自行发起 API 请求；数据由页面状态层传入。

## Web API 对齐

前端页面只能消费 Web API 事件和 REST 响应：

- `market.price`
- `market.tape_alert`
- `market.end_of_day`
- `forum.post`
- `agent.account_snapshot`
- `audit.graph`
- `audit.causal_chain`
- `runtime.tick_state`
- `runtime.agent_lifecycle`
- `system.error`
- REST `POST /api/v1/sessions`
- REST `GET /api/v1/sessions/{session_id}`
- REST `POST /api/v1/sessions/{session_id}/start`
- REST `POST /api/v1/sessions/{session_id}/pause`
- REST `POST /api/v1/sessions/{session_id}/step`
- REST `POST /api/v1/sessions/{session_id}/stop`
- REST `GET /api/v1/sessions/{session_id}/snapshot`
- REST `GET /api/v1/sessions/{session_id}/events`

字段归属：

| 页面需求 | Web API 来源 | 内部来源 |
| :--- | :--- | :--- |
| 行情、盘口聚合 | `market.price`、`snapshot.market` | `Market_Price` |
| 盘口异动和盘后披露 | `market.tape_alert`、`market.end_of_day` | `Tape_Alerts`、`End_of_Day` |
| 公开论坛消息 | `forum.post` | `Forum_Rumors` |
| Agent 资产审计视图 | `agent.account_snapshot`、`snapshot.agents` | `Account_Snapshot` |
| 拓扑图和因果链 | `audit.graph`、`audit.causal_chain`、`snapshot.audit_graph`、`snapshot.causal_chains` | `Frontend_Audit_Graph`、`Frontend_Causal_Chain` |
| 运行状态 | `runtime.tick_state`、`runtime.agent_lifecycle`、会话状态响应 | Meta-Orchestrator `control_only` 状态摘要 |

恢复规则：

- 首次进入运行页时先取 REST `GET /api/v1/sessions/{session_id}/snapshot`，再用 `from_seq=snapshot.last_seq` 连接 WebSocket。
- WebSocket 只发送 `subscribe`、`ack`、`ping` 三类客户端消息。
- 收到 `SNAPSHOT_REQUIRED` 后必须重新获取快照，不读取内部 Redis。

前端禁止：

- 下单。
- 改账。
- 改仓。
- 改冻结股。
- 注入 `thought`。
- 写入 `Forum_Rumors`。
- 读取内部 Redis。
- 读取内部 channel。
- 展示原始 `thought`、Prompt、私有记忆、Agent 原始 payload。

## 未决问题

本节记录第一版已决策略和后置问题。高判断区后续变更必须同步更新本文和 Web API 文档。

### 第一版已决

- 沙盘初始化控制台允许创建真实会话；智能体配平控件第一版只做 `agent_profile_set` 的可视化预览和本地草稿，不绕过 Web API 创建协议。
- 时间轴剧本管理第一版只读；允许本地草案预览，不保存、不发布、不写入 Chronos SSOT。
- 智能体解剖室第一版只有普通审计视图，不做内部诊断角色，不展示原始 COT。
- 漏洞白皮书的崩塌概率只展示后端或快照提供的字段；缺字段时显示 `N/A`，前端不得自行计算并伪装成系统结论。
- 图谱布局第一版由前端确定性计算；布局结果只服务展示，不回传后端，不进入 Agent。

### 后置问题

- 若要让配平控件真实影响会话创建，必须扩展 `POST /api/v1/sessions` 请求 schema，并定义字段来源、校验和审计含义。
- 若要保存时间轴草案或正式发布插桩，必须新增独立 Web API，定义权限、审计日志、未来事实可见性和恢复语义。
- 若要增加内部诊断视图，必须独立设计权限、审计日志和接口，不复用普通 Web API 事件回放。
- 若要让白皮书生成正式结论，崩塌概率和结论字段必须由后端提供，前端只负责展示和排版。
- 若要实现跨端或移动端体验，需要重新设计信息架构；第一版不做移动端卡片流。
